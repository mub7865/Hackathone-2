{{/* This template references external secret created via kubectl */}}
{{/* Secrets are NOT created in Helm to avoid committing them to Git */}}

{{/* To create secrets manually:
   kubectl create secret generic todo-secrets \
     --from-literal=DATABASE_URL="$DATABASE_URL" \
     --from-literal=JWT_SECRET="$JWT_SECRET" \
     --from-literal=OPENAI_API_KEY="$OPENAI_API_KEY" \
     -n todo-app
*/}}

{{/* Secret is referenced in backend-deployment.yaml */}}
{{/* This file is intentionally empty - it serves as documentation only */}}
